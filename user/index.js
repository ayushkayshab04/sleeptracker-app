const userService = require('./user.service');
const userContrller = require('./user.controller');
const userRoute = require('./user.routes');

module.exports = {
  userService,
  userContrller,
  userRoute,
};
